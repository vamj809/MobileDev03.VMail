<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="MobileDev03.VMail.Views.AddMailPage"
             xmlns:models="clr-namespace:MobileDev03.VMail.Models"
             x:DataType="models:Mail">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Enviar" Order="Primary" Priority="0" Command="{Binding SendMailCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid Padding="5 10 5 10"
                      RowDefinitions="Auto,Auto,Auto,*"
                      ColumnDefinitions="*">
            <!-- Recipient Row -->
            <StackLayout Grid.Row="0" Orientation="Horizontal">
                <Label Text="A:"
                    HorizontalOptions="Start" VerticalOptions="CenterAndExpand"/>
                <Entry Text="{Binding Recipient}"
                    HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand"/>
                <Label Text="\/"
                    HorizontalOptions="End" VerticalOptions="CenterAndExpand"/>
            </StackLayout>
            
            <!-- Sender Row -->
            <StackLayout Grid.Row="1" Orientation="Horizontal">
                <Label Text="De:"
                    HorizontalOptions="Start" VerticalOptions="CenterAndExpand"/>
                <Entry Text="{Binding Sender}"
                    HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand"/>
                <Label Text="\/"
                    HorizontalOptions="End" VerticalOptions="CenterAndExpand"/>
            </StackLayout>
            
            <!-- Subject Row -->
            <Entry Grid.Row="2" Placeholder="Asunto" Text="{Binding Subject}"
                    HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand"/>
            <!-- Content Row -->
            <Editor
                Grid.Row="3"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand"
                Text="{Binding Body}"
                Placeholder="Escriba el contenido de su correo..."/>
        </Grid>
    </ContentPage.Content>
</ContentPage>